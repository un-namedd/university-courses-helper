# Supabase project credentials (Project Settings -> API).
# Cloud sync (sign-in, saved plans, share links) is optional: leave these blank
# and the app still works fully with localStorage-only plans.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
